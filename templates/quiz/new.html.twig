{% extends 'base.html.twig' %}

{% block title %}New-Quiz{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card mt-5">
                <div class="card-body">
                    <h1 class="card-title text-center mb-4">Create New Quiz</h1>
                    {{ include('quiz/_form.html.twig') }}
                    <a href="{{ path('app_quiz_index') }}" class="btn btn-secondary btn-block mt-3">Back to List</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
